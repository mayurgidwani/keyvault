{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for KeyVault.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
